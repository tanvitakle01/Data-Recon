# Executive Dashboard — UI spec implementation notes

This folder contains UI-only primitives used by `ReconciliationPage`.

- HeroSection
  - SideRays (atmospheric rays) inside hero only
  - GradientText (subtle enterprise gradient)
  - ScrollVelocity (trust ticker)

- Upload section
  - BorderGlow wrapper (hover-only glow)

- Summary section
  - SummaryGrid / MetricCard + Counter (single count-up)

