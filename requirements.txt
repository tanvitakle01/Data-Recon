# Direct runtime dependencies, pinned to the versions this app is developed
# against. Transitive packages (anyio, h11, certifi, python-dateutil, …) are
# resolved by pip and deliberately not pinned here.

# --- Web server ---
fastapi==0.137.1
starlette==1.3.1
uvicorn==0.49.0
pydantic==2.13.4
# FastAPI needs this to parse the multipart uploads every /api route accepts.
python-multipart==0.0.32

# --- Tabular data / Excel ---
pandas==3.0.3
numpy==2.4.6
openpyxl==3.1.5
# pandas.read_excel dispatches to xlrd for legacy .xls uploads.
xlrd==2.0.2

# --- LLM (Azure AI Foundry, OpenAI-compatible endpoint) ---
openai==1.109.1
# DefaultAzureCredential; on Render this resolves via the service-principal
# environment variables (AZURE_CLIENT_ID / AZURE_TENANT_ID / AZURE_CLIENT_SECRET).
azure-identity==1.25.3
# Uses the OS trust store for TLS, so corporate/proxy root CAs are honoured.
truststore==0.10.4
