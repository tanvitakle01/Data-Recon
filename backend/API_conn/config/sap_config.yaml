s4:
  enabled: true
  base_url: "https://ALINHANA21.bristlecone.com:44307"
  service: "API_SALES_ORDER_SRV"
  username: "TRAIN_51_S21"
  password: "Integration@123"
  # TLS verification is ON by default. If this host's cert chains to a
  # corporate/self-signed CA not in the default trust store, point
  # ca_bundle at a PEM/CRT file. skip_tls_verify is a non-default, explicit
  # escape hatch — leave it unset/false outside of throwaway local dev.
  # TRAIN_51_S21 presents a self-signed cert with no corporate CA bundle
  # available (2026-08-15, confirmed with user) — skip_tls_verify enabled
  # for this training/sandbox host until a proper ca_bundle can be supplied.
  ca_bundle: null
  skip_tls_verify: true

ibp:
  enabled: true
  base_url: "https://my400230-api.scmibp.ondemand.com"
  service: "IBP/PLANNING_DATA_API_SRV"
  username: "IBP_FILE_CI"
  password: "IbpApiconn@123456789"
  ca_bundle: null
  skip_tls_verify: false


  #planning_area: "HPA2508"
  #key_figure: "I_SALESORDERREQUEST"
  #planning_level: "I_DAYPRODLOCDEMAND"
 
 